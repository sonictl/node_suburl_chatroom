# 轻量级聊天室 - 需求说明书 v1.0

## 一、技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| 后端 | Node.js + Express | 提供页面服务和API |
| 实时通信 | Socket.io | WebSocket封装，自带断线重连和房间管理 |
| 模板引擎 | EJS | 渲染主页面 |
| CSS框架 | TailwindCSS + DaisyUI | CDN引入，轻量级，使用 Cupcake 主题 |
| 数据存储 | 内存 | 不持久化历史消息，仅存储当前房间在线用户 |
| 环境配置 | dotenv | 通过 `.env` 文件配置端口号 |

## 二、核心功能

### 2.1 房间机制

- **路由格式**：`{{base_url}}/{{roomId}}`
  - 示例：`chat.example.com/gaming` 或 `chat.example.com/朋友聚会`
- `roomId` 支持字母、数字、中文（URL编码）
- 不同房间完全隔离（用户、消息互不干扰）

### 2.2 用户认证流程

1. 首次进入房间 → 弹出昵称输入框
2. 提交昵称后校验：
   - 同一房间内昵称唯一 → 冲突则弹窗提示"昵称已被占用"
   - 昵称长度：1-16字符，不允许空白
3. 通过后：昵称存入 `localStorage`（按房间分别存储，格式：`nickname_{{roomId}}`）
4. 下次进入同一房间 → 自动使用已保存昵称直接登录
5. 提供"更换昵称"按钮（聊天界面内，点击后重新走流程）

### 2.3 消息系统

| 消息类型 | 实现方式 | 存储 |
|----------|----------|------|
| 普通聊天 | 广播给房间内所有人 | 不持久化 |
| 私聊消息 | 仅发送给指定用户（前端标记"私聊"样式） | 不持久化 |
| 系统通知 | 进入/离开等，特殊样式 | 不持久化 |

**消息格式：**

```javascript
{
  id: "uuid",           // 唯一标识
  type: "chat" | "private" | "system",
  from: "昵称",
  to: "昵称" | null,    // 私聊时有值
  content: "消息内容",
  timestamp: Date.now(),
  roomId: "房间名"
}
```

**2.4 界面布局与样式**

```text
┌─────────────────────────────────────────────────┐
│  [房间名: gaming]          [在线人数: 5] [更换昵称] │
├───────────────────────────┬─────────────────────┤
│                           │                     │
│   ┌─────────────────────┐ │   在线用户列表       │
│   │ 张三 [19:30:15]    │ │   ◦ 张三 (自己)      │
│   │ ┌───────────────┐  │ │   ◦ 李四            │
│   │ │ 大家好！       │  │ │   ◦ 王五            │
│   │ └───────────────┘  │ │                     │
│   │                     │ │   💬 私聊标记：      │
│   │   ┌───────────────┐ │ │   点击用户昵称 → @ │
│   │   │ 李四 [19:30:20]│ │ │   (自动填入输入框)  │
│   │   │ 嗨～          │ │ │                     │
│   │   └───────────────┘ │ │                     │
│   │                     │ │                     │
│   │   [正在输入...]     │ │                     │
│   └─────────────────────┘ └─────────────────────┘
├─────────────────────────────────────────────────┤
│  [ 输入框... ]                          [ 😊 ] [发送] │
└─────────────────────────────────────────────────┘
```

**布局规则：**

- 自己的消息：气泡靠右，浅绿色背景（`#c8e6c9`）
- 他人的消息：气泡靠左，更浅绿色背景（`#e8f5e9`）
- 私聊消息：橙色边框标记，显示"私聊给 xxx"或"私聊 from xxx"前缀
- 系统消息：居中、斜体、浅灰色
- 本人消息不显示发送者名称

**2.5 消息列表行为**

- 新消息到达 → 自动滚动到底部
- 用户主动滚动查看历史时不强制滚动（当滚动到距离底部<100px时恢复自动滚动）
- 新消息到达时，如果页面不在焦点，标题栏闪烁显示 "📩 New message"

**2.6 Emoji支持**

- 集成 emoji-picker-element 轻量组件
- 点击输入框左侧 😊 按钮弹出选择面板
- 点击 Emoji 自动插入到输入框

**2.7 私聊功能**

**触发方式：**

- 点击在线列表中的用户昵称 → 输入框自动添加 @用户名
- 发送时识别：若消息以 @用户名 开头，则转换为私聊消息
- 也可以通过 /w 用户名 消息内容 命令发送

**私聊样式：**

- 发送方看到：[私聊给 李四] 你的消息
- 接收方看到：[私聊 from 张三] 消息内容
- 房间其他用户看不到该消息

**2.8 连接状态指示**

- 顶部显示状态指示器：
  - 🟢 已连接
  - 🟡 重连中...
  - 🔴 断开连接
- Socket.io 自动重连时显示提示，恢复后自动隐藏

**2.9 打字指示器**

- 当用户在输入框敲字时 → 防抖触发（停止输入1秒后清除）
- 实时显示在聊天区域底部
- 显示格式：张三 正在输入...

**2.10 换行支持**

- 按 Shift + Enter 可换行
- 消息气泡使用 `white-space: pre-wrap` 保留换行符

## 三、数据模型（内存存储）

```javascript
// 房间结构
rooms = Map {
  "roomId": {
    users: Map {           // 用户会话映射
      "socketId": {
        nickname: "张三",
        socket: socket对象
      }
    },
    nicknamesSet: Set(["张三", "李四"]),  // 快速查重
    mutedIPs: Map {        // 禁言记录（IP -> 到期时间）
      "::1": { until: 1234567890 }
    }
  }
}
```

## 四、Socket.io 事件列表

| 事件名          | 方向            | 说明                         |
| --------------- | --------------- | ---------------------------- |
| join            | client → server | 加入房间（携带昵称）         |
| join-error      | server → client | 昵称冲突回调                 |
| join-success    | server → client | 加入成功回调                 |
| leave-room      | client → server | 主动离开房间（更换昵称时）   |
| chat-message    | client → server | 发送普通消息                 |
| private-message | client → server | 发送私聊消息                 |
| message         | server → client | 广播/私聊消息给客户端        |
| user-join       | server → client | 用户进入通知（更新在线列表） |
| user-leave      | server → client | 用户离开通知                 |
| online-users    | server → client | 更新在线用户列表             |
| typing          | client → server | 打字状态                     |
| typing-status   | server → client | 转发打字状态                 |
| mute-user       | client → server | 管理员禁言用户               |
| disconnect      | client → server | 断开连接（自动清理）         |

## 五、非功能性要求

1. **轻量级**：代码量小，易于二次开发
2. **零依赖持久化**：不依赖数据库
3. **断线重连恢复**：重连后自动恢复房间和昵称状态

## 六、附加功能要求

1. emoji 选择面板（emoji-picker-element）
2. 消息搜索功能（高亮匹配、上下导航）
3. @消息通知（浏览器 Notification）
4. 管理员禁言用户功能（封IP，1分钟/5分钟/10分钟可选）
5. 新消息标题闪烁（页面失焦时）
6. 换行符支持（Shift+Enter）
